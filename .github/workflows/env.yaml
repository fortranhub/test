on:
  - push
  
jobs:
  job:
    runs-on: 
      - ubuntu-latest
      
    steps:
      - name: debug
        run: echo ${{ secrets.ENV }} > file
        
      - name: debug2
        id: abcd
        run: echo "ESIM=$(cat file)" >> $GITHUB_OUTPUT

      - name: debug3
        run: echo ${{ steps.abcd.outputs.ESIM }}
