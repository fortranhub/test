on:
  pull_request:
  
jobs:
  job:
    runs-on: 
      - ubuntu-latest
      
    steps:
      - name: debug
        run: echo "hello world!"
      - name: debug2
        id: out
        run: echo HELLO=$(cat hello) >> $GITHUB_OUTPUT
      - name: debug3
        run: ${{ steps.out.outputs.HELLO }}
